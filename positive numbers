# Define a function to print all positive numbers in a list
def print_positive_numbers(numbers):
    # Create an empty list to store the positive numbers
    positive_numbers = []

    # Iterate through the list and add all positive numbers to the new list
    for num in numbers:
        if num > 0:
            positive_numbers.append(num)

    # Print the list of positive numbers
    print(positive_numbers)

# Example usage
list1 = [12, -7, 5, 64, -14]
print_positive_numbers(list1) # Output: [12, 5, 64]

list2 = [12, 14, -95, 3]
print_positive_numbers(list2) # Output: [12, 14, 3]
